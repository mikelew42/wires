Problem with camelCase:

.get_product() --> returns product instance
.get_Product() --> returns Product class

With camel case, we'd only have .getProduct(), and you're limited to your casing.

But, do I want to put An_Underscore_Between each word of a class?